---
description: 'Error handling, logging, and resilience (mobile)'
globs: ['**/*.{ts,tsx}', '**/store/**/*.ts', '**/lib/**/*.ts']
alwaysApply: false
priority: 92
---

# Resilience & logging

## 1. Logging

- **Never** raw `console.error` in application code — use **`logger.error(message, error, context)`** from `@/lib/logger`.
- `logger.debug` / `logger.info` may use `console.log` in development only; keep messages structured and grep-friendly.

## 2. Errors in UI

- Route-level failures: rely on Expo Router **`ErrorBoundary`** (see `src/app/_layout.tsx`) and render a recoverable fallback.
- TanStack Query: always consider **`isError` / `error`** in UI for user-visible queries; log unexpected failures via `logger` or `QueryClient` `meta` handlers when you add them.

## 3. Catch blocks

- **Silent catch is forbidden.** Either log with module context + rethrow, or map to a typed domain error the UI can show.
- Use `unknown` in `catch` and narrow before use.

## 4. Secrets & env

- **Never** read `process.env.*` directly in app code — runtime config **always** flows through the Zod-validated **`src/env.ts`** object.
- **Never** put billing-capable secrets or API keys in **`EXPO_PUBLIC_*`** variables — they are inlined into the JS bundle at build time and are **not** secret.
- Adding a var = edit `.env.example` **and** the `src/env.ts` Zod schema, then **restart Metro** (env is read once at build).
- Tokens belong in **`expo-secure-store`**, **never** AsyncStorage and **never** in a Zustand `persist` `partialize` — a leaked token there is a plaintext leak (see `.cursor/brain/SKELETONS.md`).

## 5. Boundary adapter

- A response is parsed **once**, at the boundary: `safeFetch` / `safeFetchQueryFn` + a Zod schema (`src/lib/api/safeFetch.ts`; live example `src/lib/api/_exampleSafeQuery.ts`). AsyncStorage and `expo-secure-store` reads go through `Schema.safeParse` the same way.
- Defaults and renames live in the schema or a mapper next to it — `.default()`, `.catch()`, `.transform()`, or a `toViewModel(dto)` beside the query module (`src/features/<name>/api/`, placement in `fsd-layers.mdc`) — never in a component.
- Screens, widgets and feature hooks receive the parsed view model: they never see a raw DTO and never supply their own fallbacks (`data?.x ?? ''` in JSX is the smell).
- A schema change is a payload-contract change: the Approval Law applies (`workflow.mdc`, the contract / public-export row).
