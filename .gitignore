# Dependencies
node_modules/

# Build output
.expo/
dist/
web-build/
coverage/
# Stryker mutation testing (the incremental report is a local/CI cache, not a source)
.stryker-tmp/
reports/
.bundle-check/
.atlas/
.atlas-android/

# Native — regenerated by `expo prebuild`. Do NOT commit in managed workflow.
ios/
android/

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
*.log

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
# Tracked because README documents these tasks — an untracked file makes that
# claim false for every fork. Each task calls an npm script rather than spelling
# the gate out, so it cannot drift from `verify`.
!.vscode/tasks.json

# AI agents (local scratch)
.claude/
CLAUDE.local.md

# .claude/commands/ holds the repo's own agent commands and IS tracked. The
# directory itself has to be re-included first: `.claude/` is ignored above (and
# globally), and git will not descend into an ignored directory to see a negation
# inside it. Machine-local settings stay untracked.
!.claude/
!.claude/commands/
!.claude/commands/**
.claude/settings.local.json
.claude/settings.json

# Metro / Jest
.metro-cache/
.jest-cache/

# Maestro
.maestro/reports/

# TypeScript / tooling caches
*.tsbuildinfo
.eslintcache
.prettiercache

# Local signing / store credentials (never commit)
*.jks
*.keystore
*.p12
*.mobileprovision

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# .cursor/ is tracked in this template (personal repo) — override the global
# ghost-mode ignore (~/.gitignore_global) so plain `git add` works for agents.
!.cursor/
!.cursor/**
