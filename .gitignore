# Dependencies
node_modules/

# Build output
.expo/
dist/
web-build/
coverage/
.bundle-check/
.atlas/
.atlas-android/

# Native — regenerated by `expo prebuild`. Do NOT commit in managed workflow.
ios/
android/

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
*.log

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
# Tracked because README documents these tasks — an untracked file makes that
# claim false for every fork. Each task calls an npm script rather than spelling
# the gate out, so it cannot drift from `verify`.
!.vscode/tasks.json

# AI agents (local scratch)
.claude/
CLAUDE.local.md

# Metro / Jest
.metro-cache/
.jest-cache/

# Maestro
.maestro/reports/

# TypeScript / tooling caches
*.tsbuildinfo
.eslintcache

# Local signing / store credentials (never commit)
*.jks
*.keystore
*.p12
*.mobileprovision

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# .cursor/ is tracked in this template (personal repo) — override the global
# ghost-mode ignore (~/.gitignore_global) so plain `git add` works for agents.
!.cursor/
!.cursor/**
