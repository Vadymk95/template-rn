/**
 * Tab bar tint aligned with NativeWind light `foreground` (`global.css` / Tailwind theme).
 * Keep in sync when rebranding — do not sprinkle raw hex in layouts.
 */
export const TAB_BAR_ACTIVE_TINT = '#0a0a0a' as const;
