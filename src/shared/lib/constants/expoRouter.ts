/**
 * Expo Router segment names — must match the file tree under `src/app/`.
 * Change filenames first, then update these constants and run `npx expo customize tsconfig` if typed routes drift.
 */
export const EXPO_ROUTER = {
    rootTabGroup: '(tabs)',
    rootStackInitialRoute: '(tabs)'
} as const;
