name: CI

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

permissions:
    contents: read

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    verify:
        name: Typecheck, lint, test
        runs-on: ubuntu-latest
        timeout-minutes: 15
        permissions:
            contents: read

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version-file: .nvmrc
                  cache: npm

            # Node 24.15.0 (current LTS) bundles npm 11.12.1, which regressed
            # peerOptional handling (npm/cli#9249, 2026-04-17) — peerOptional
            # deps marked extraneous and removed. Combined with the long-standing
            # cross-platform optional-binaries bug (npm/cli#4828) this means
            # lock files generated on macOS arm64 fail Linux CI with EUSAGE
            # "Missing: <pkg>-linux-x64-gnu from lock file" for native bindings
            # (@expo/ngrok-bin, @unrs/resolver-binding, etc.). Both issues are
            # fully restored in npm 11.14.0 (2026-05-06) via arborist 9.5.0.
            # Pin to ^11.14.0 explicitly — npm@latest would track future majors
            # that may regress again.
            - name: Upgrade npm to ^11.14 (cross-platform optional binaries fix)
              run: npm install -g npm@^11.14.0

            - name: Install
              run: npm ci

            - name: Verify core contract (typecheck, lint, format, coverage)
              run: npm run verify

            # Expo doctor checks live external SDK/package state and can go red with
            # no code change (drift). Advisory, not a PR gate (tooling /consilium
            # 2026-07-21): it still runs and surfaces in the log, but does not block.
            - name: Expo doctor (SDK health, advisory)
              run: npm run doctor
              continue-on-error: true

    dependency-review:
        name: Dependency review
        runs-on: ubuntu-latest
        if: github.event_name == 'pull_request'
        timeout-minutes: 5
        permissions:
            contents: read
            pull-requests: read

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Review dependency changes for vulnerabilities / license issues
              uses: actions/dependency-review-action@v4
